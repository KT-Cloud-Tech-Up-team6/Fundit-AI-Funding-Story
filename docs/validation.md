# 로컬 서비스 통합 검증 — 2026-09-15

## 확인한 범위

- Python 3.12.14, LangGraph 1.2.11, uv.lock 기준 로컬 API/워커 실행
- 실제 Google 모델의 강점 정리·채팅 응답·원고·이미지 생성
- 3종 선물 데이터: 이미지 10/10 성공. 독립된 제품 1·2·3개 이미지 확인
- FE에서 입력한 1종 선물 데이터: 생성 이미지 8/8 성공, 미등록 카드 2개는 input_required
- FE → Next 서버 프록시 → Spring BE → Python AI의 업로드/채팅 SSE/확인/생성/폴링
- 문구 편집 및 BE 저장, 새로고침 후 문구 유지
- 이전 revision 저장은 409, 다른 사용자 접근은 403
- 문제 카드만 실제 부분 재생성. 나머지 블록 JSON과 사용자가 편집한 hero.detail-0 문구 보존
- 수정 후 적용 문서의 브라우저 렌더러: 이미지 로딩 완료 후 overflow/렌더 오류 없음
- 프로젝트 정보 HTML 편집·저장, 생성 본문과 860px 경계 일치 확인

테스트 데이터는 가상 제품이다. 1종 UI 테스트의 하단 정보는 별도 편집 기능을 통해 입력·저장했으며 AI가 생성한 정보가 아니다.

## 테스트

- AI 18개: 출력 형식 오류 피드백/횟수 제한, 공급자 429 대기, SSE 첫 토큰 전 재시도와 전송 후 중복 금지, 슬롯 누락, 가격 비추정, 프로젝트 범위, 중복 요청, 1·2·3종 선물, 실패 이미지 재사용, 체크포인트 예외 후 재개, 부분 블록 보존
- FE 71개: 기존 테스트 + 사용자/프로젝트 캐시 분리, stale 복원 거부, 분할 SSE 프레임
- FE TypeScript/변경 파일 ESLint/프로덕션 build
- BE project-service 전체 테스트 통과 후 신규 3개 재실행 및 bootJar
- AI Docker build 및 컨테이너 안 API·16블록 템플릿 import

AI 테스트는 별도 funding_ai_test DB를 사용한다. 체크포인트 재개는 단계 예외를 주입해 검증했으며 OS 강제 종료/다중 호스트 장애 실험을 대신 완료라고 주장하지 않는다.

## 수정한 연동 결함

- Java HTTP/2 cleartext upgrade로 multipart 업로드가 거부되던 문제 → HTTP/1.1
- 후보 편집 후 적용 시 수정 소실 → 후보 읽기 전용, 적용 문서에서 편집
- 부분 재생성 전 미저장 편집 → 먼저 BE 저장, 해당 revision 기준 생성
- 미등록 선물 이미지 상태 미저장 → 루프 종료 시 상태 저장, 1·2종 회귀 테스트
- 문제 카드에 긴 확인 설명을 직접 바인딩 → 생성 시 슬롯별 짧은 문구 작성
- 이미지 요청의 출력 의도 불명확 → 이미지 생성 지시 명시, 실패 슬롯 재실행
- 이미지 URL 준비 중 오류 표시 → 로딩 상태와 실제 실패 분리
- 테스트 작업과 실워커 DB 혼용 → 테스트 DB 분리
- SSE가 첫 토큰 전에 실패할 때 SDK iterator 예외가 재시도 밖에 있던 문제 → 첫 출력 전 오류만 제한 재시도

## 한계와 후속 검토

- LangSmith 연결 코드는 있으나 API 키가 없어 외부 trace 화면 조회는 미검증
- 운영 S3 권한·Gateway 실제 로그인·공개 이미지/CDN·배포 경로는 관련 팀 환경에서 연동 검증 필요
- CI 파일은 작성했으나 GitHub 실행·push는 하지 않음
- 이미지 슬롯 채움과 기능 동작이 다양한 제품의 품질을 보장하지 않는다. 일부 생성 이미지의 작은 레터링·제품 외형 차이 등은 추가 품질 평가 대상이며, 자동 LLM 품질 검사나 교정은 넣지 않음
- Figma 방향을 참고한 내부 연동 화면이며 최종 UX·모바일 편집·게시 문안은 디자인/기획 검토 대상
- 현재 강점 기반 블록 선택은 단일 생활가전 카탈로그의 규칙 방식. 다른 카테고리 일반화 완료가 아님

실행 관측 ID(로컬 DB에만 존재): 3종 `4e39a209-b927-46ab-bbd4-840f78c6e9db`, 1종 `5a5b587f-89ce-40a4-ba08-a5b23d5140b5`, 문제 카드 부분 재생성 `3cc432bd-82fe-4707-98ab-f099fb0761e5`.

## 2026-09-15 · 신규 프로젝트 UI부터 생성까지

이전 사전 등록 UUID 검증에 이어, 실제 신규 등록 폼에서 LUMI S1을 생성했다.

- 프로젝트: 01a0a289-bde2-75ae-98d1-f32745540e1c
- run: d027c54d-545a-4d53-9d0b-103e0f2f495e
- UI 경로: 목록 → 신규 등록 → 기본정보/리워드 저장 → AI 작성 → 이미지 업로드 → 강점 확인 → 생성 → 결과 적용 → 문구 편집/저장 → 목록 재진입 → 미리보기
- 생성: 초기 부분 성공 후 UI 재시도, 최종 succeeded. 이미지 8개 성공, 미입력 리워드 2개는 input_required 유지.
- BE 문서 revision 2, hero.detail-0의 사용자 편집 문구 보존 확인.
- 카테고리/목표금액/리워드명/가격/설명 자동 전달 확인.
- FE 71개, AI 18개 테스트 통과 및 FE 빌드 성공. BE 전체 테스트와 신규 등록 정상/롤백 DB 통합 테스트 통과.
- 미입력 등록 요청은 400. 신규 등록 시 일부 리워드 실패하면 프로젝트까지 롤백.
- 표시용 고정 안내문 최종 확정, 실제 로그인/Gateway, 리워드 옵션, 심사·공개 게시와 전체 품질 평가는 이번 완료 범위에 포함하지 않는다.

브라우저 참고: Chrome의 한글 문서 확장이 html에 data-hwp-extension 속성을 주입해 개발 모드 hydration 경고를 남겼다. 페이지 생성/저장/재열기는 정상이며, 이 경고를 숨기기 위해 앱의 hydration 검사를 비활성화하지 않았다.

## 2026-09-15 대화형 진입 복구

[복구 기준과 체크리스트](../../../docs/service-ux-restoration.md). 신규 세션에서 빈 설명→추가 질문→이미지 첨부→종합 요약→강점 삭제/정렬→새로고침→확인→실제 생성→FE 결과 불러오기/BE revision 1 저장을 확인했다. 생성 `1b1adee7-2643-4b38-80e8-17e54a60d7d6`: 성공 이미지 7개, 미등록 선물 슬롯 2개. 429 두 슬롯은 UI 재시도로 성공. AI 20개 테스트, FE 71개 테스트, BE 전체 테스트 및 FE build 통과. 전체 디자인/카테고리 마스터의 완료 선언은 아니다.

## 2026-09-15 명시 입력 수정 → 재생성

[복구 문서](../../../docs/service-ux-restoration.md)의 후속 검증. `input_changes`로 최신 사용자 인용/원문 값만 적용한다. 정상가 199,000원·할인가 169,000원·완제품 1개·말투·예산·일정이 실제 생성 입력과 결과에 반영됐다. run `87d87c87-cb93-45b6-add5-36194a6dbf10` succeeded: 이미지 7개 성공, 미등록 선물 2칸 input_required. UI 불러오기 BE revision 2 저장 및 새로고침/채팅 복원 확인. AI 28개 테스트/Ruff, FE 71개 테스트/타입/변경 파일 린트 통과. 전체 Figma 세부 대조와 카테고리 마스터 확정은 별도 잔여 사항이다.

## 2026-09-16 · AI API 0.2.0 PNG·텍스트 출력 계약

이번 검증은 FE·디자인 코드를 수정하지 않고 AI 저장소만 대상으로 했다.

- 기존 FE의 `ProjectStoryForm → StoryEditor → FundingStoryModal` 호출 지점을 읽기 전용으로 대조하고 [호출 대응표](frontend-call-contract.md)를 작성했다.
- 전체 호출 순서를 TestClient로 실행했다: 자산 업로드 → 세션 → 메시지 → SSE → 요약/강점 확인 → 전체 생성 → 상태 조회 → 문구 override → 블록별 PNG+텍스트 export → BE 저장 revision 모의 commit.
- export idempotency, 다른 payload 충돌, stale 입력 revision, 부분 성공 run 거부, 알 수 없는 문구 슬롯 거부, 프로젝트 자산 격리를 확인했다.
- commit 후 run의 임시 디자인·입력 snapshot 삭제와 실행 체크포인트 정리를 확인했다. 동일 본문 revision 재호출은 성공하고 다른 revision은 409다.
- PNG 응답은 PNG signature, `image/png`, 블록 수·860px 폭·순서·대체 텍스트로 검증했다. Pretendard의 시각적 픽셀 대조는 기존 Konva 레퍼런스와 별도의 후속 검증이다.
- `uv run ruff check src tests scripts` 통과, `uv run pytest -q` 30개 통과. Starlette TestClient의 anyio alias deprecation warning 1건은 상위 의존성 경고이며 테스트 실패가 아니다.
- 로컬 uvicorn `127.0.0.1:58011`에서 `/health` 200, 인증 없는 `/v1/sessions/latest` 401, OpenAPI의 export/get/commit 경로 노출을 확인했다.

이번 결과를 실제 Figma 화면, Tiptap 불러오기, BE 영구 본문 저장 완료로 확대 해석하지 않는다. FE/BE 변경은 인계 항목이다. 공통 크라우드 펀딩 안내 key는 `pending-v1`이며 기획 확정 전 운영 문안이 아니다. Docker build와 GitHub CI 결과는 각각 로컬 빌드 완료 및 원격 실행 여부를 따로 기록한다.
